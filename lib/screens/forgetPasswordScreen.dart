import "package:flutter/material.dart";

class MyWidget extends StatelessWidget {
    static String router = "/resetpassword";
  const MyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
